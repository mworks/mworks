<?xml version="1.0"?>
<marionette_info>
  <expected_messages>
    <message type="contains">All parameters to function DISPLAY_BOUNDS() must be strings</message>
    <message type="starts_with">ERROR: Failed to parse experiment</message>
  </expected_messages>
  <expected_events>
    <!-- The experiment shouldn't load, so we don't expect the normal state-system events -->
  </expected_events>
</marionette_info>
