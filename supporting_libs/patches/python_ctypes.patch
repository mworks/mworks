diff -ur Python-3.10.2/setup.py Python/setup.py
--- Python-3.10.2/setup.py	2022-01-13 13:52:14.000000000 -0500
+++ Python/setup.py	2022-02-18 11:33:12.000000000 -0500
@@ -2267,8 +2267,12 @@
                                sources=['_ctypes/_ctypes_test.c'],
                                libraries=['m']))
 
-        ffi_inc = sysconfig.get_config_var("LIBFFI_INCLUDEDIR")
-        ffi_lib = None
+        #ffi_inc = sysconfig.get_config_var("LIBFFI_INCLUDEDIR")
+        #ffi_lib = None
+        # Use our libffi
+        ffi_inc = sysconfig.get_config_var('INCLUDEDIR')
+        ext.library_dirs.append(sysconfig.get_config_var('LIBDIR'))
+        ffi_lib = 'ffi'
 
         ffi_inc_dirs = self.inc_dirs.copy()
         if MACOS:
