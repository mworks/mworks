diff -ur numpy-1.17.0/numpy/distutils/system_info.py numpy/numpy/distutils/system_info.py
--- numpy-1.17.0/numpy/distutils/system_info.py	2019-07-26 10:10:50.000000000 -0400
+++ numpy/numpy/distutils/system_info.py	2019-08-13 12:54:04.000000000 -0400
@@ -2149,7 +2149,8 @@
         libraries = [lib.strip().lower() for lib in libraries]
 
         if (sys.platform == 'darwin' and
-                not os.getenv('_PYTHON_HOST_PLATFORM', None)):
+                #not os.getenv('_PYTHON_HOST_PLATFORM', None)):
+                True):
             # Use the system BLAS from Accelerate or vecLib under OSX
             args = []
             link_args = []
@@ -2165,7 +2166,8 @@
                 if intel:
                     args.extend(['-msse3'])
                 else:
-                    args.extend(['-faltivec'])
+                    #args.extend(['-faltivec'])
+                    pass
                 args.extend([
                     '-I/System/Library/Frameworks/vecLib.framework/Headers'])
                 link_args.extend(['-Wl,-framework', '-Wl,Accelerate'])
@@ -2175,7 +2177,8 @@
                 if intel:
                     args.extend(['-msse3'])
                 else:
-                    args.extend(['-faltivec'])
+                    #args.extend(['-faltivec'])
+                    pass
                 args.extend([
                     '-I/System/Library/Frameworks/vecLib.framework/Headers'])
                 link_args.extend(['-Wl,-framework', '-Wl,vecLib'])
