#include "Base.xcconfig"

ARCHS = arm64
DEBUG_INFORMATION_FORMAT = dwarf-with-dsym
IPHONEOS_DEPLOYMENT_TARGET = 18.0
LD_RUNPATH_SEARCH_PATHS = $(inherited)
HEADER_SEARCH_PATHS = $(inherited) $(BUILT_PRODUCTS_DIR)/$(CURRENT_ARCH)/include
LIBRARY_SEARCH_PATHS = $(inherited) $(BUILT_PRODUCTS_DIR)/$(CURRENT_ARCH)/lib
SDKROOT = iphoneos
SUPPORTS_MACCATALYST = NO
TARGETED_DEVICE_FAMILY = 2
VALIDATE_PRODUCT = YES

#include? "iOS_CodeSigning.xcconfig"
